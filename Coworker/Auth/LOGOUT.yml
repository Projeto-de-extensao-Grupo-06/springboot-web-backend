info:
  name: LOGOUT
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
